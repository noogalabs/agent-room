{
  "name": "agent-room",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "node scripts/check-test-workspaces.mjs && npm -ws run test",
    "build": "npm -ws run build --if-present",
    "build:ordered": "npm run build -w packages/shared && npm run build -w packages/upstash-client && npm run build -w packages/room-persistence && npm run build -w apps/room-server && npm run build -w apps/local-server && npm run build -w apps/hosted-agent && npm run build -w apps/mcp && npm run build -w apps/starter && npm run build -w apps/web",
    "dev:web": "npm -w apps/web run dev"
  },
  "devDependencies": {
    "@vercel/node": "^5.7.13",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1040.0"
  }
}
